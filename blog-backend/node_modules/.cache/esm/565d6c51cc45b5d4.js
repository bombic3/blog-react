let jwt,User;_cc4‍.x([["default",()=>_cc4‍.o]]);_cc4‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_cc4‍.w("../models/user",[["default",["User"],function(v){User=v}]]);


const jwtMiddleware = async (ctx, next) => {
  const token = ctx.cookies.get('access_token');
  if (!token) return next(); // 토큰이 없을 때
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    ctx.state.user = {
      _id: decoded._id,
      username: decoded.username,
    };
    // 토큰의 남은 유효 기간이 3.5일 미만이면 재발급
    const now = Math.floor(Date.now() / 1000);
    if (decoded.exp - now < 60 * 60 * 24 * 3.5) {
      // const user = await User.findById(decoded._id);
      const user = await User.findById(decoded._id);
      const token = user.generateToken();
      ctx.cookies.set('access_token', token, {
        maxAge: 1000 * 60 * 60 * 24 * 7, // 7일
        httpOnly: true,
      });
    }
    // 콘솔에 토큰 정보를 출력하는 코드는 토큰이 만료되기 전 재발급해 주는 기능 구현해주고 나서 지울것임
    // console.log(decoded);
    return next();
  } catch (e) {
    // 토큰 검증 실패
    return next();
  }
};

_cc4‍.d(jwtMiddleware);
