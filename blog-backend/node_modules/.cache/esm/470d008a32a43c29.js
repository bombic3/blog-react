let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_32a‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_32a‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_32a‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_32a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_32a‍.w("./api",[["default",["api"],function(v){api=v}]]);_32a‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







// 토큰 검증하기 (미들웨어 적용)


// 가짜 데이터 불러오기
// import createFakeData from './createFakeData';

// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
    // createFakeData();
  })
  .catch((e) => {
    _32a‍.g.console.error(e);
  });

// const api = require('./api');

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
// app 에 (토큰 검증)미들웨어 적용하기
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _32a‍.g.console.log('Listening to port %d', port);
});
